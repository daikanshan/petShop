<%= form_for(@purchaser_list) do |f| %>
  <% if @purchaser_list.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@purchaser_list.errors.count, "error") %> prohibited this purchaser_list from being saved:</h2>

      <ul>
      <% @purchaser_list.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
