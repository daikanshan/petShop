<p id="notice"><%= notice %></p>

<h1>Listing Purchaser Lists</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @purchaser_lists.each do |purchaser_list| %>
      <tr>
        <td><%= link_to 'Show', purchaser_list %></td>
        <td><%= link_to 'Edit', edit_purchaser_list_path(purchaser_list) %></td>
        <td><%= link_to 'Destroy', purchaser_list, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New List', new_purchaser_list_path %>
