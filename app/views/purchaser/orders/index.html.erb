<p id="notice"><%= notice %></p>

<h1>Listing Purchaser Orders</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @purchaser_orders.each do |purchaser_order| %>
      <tr>
        <td><%= link_to 'Show', purchaser_order %></td>
        <td><%= link_to 'Edit', edit_purchaser_order_path(purchaser_order) %></td>
        <td><%= link_to 'Destroy', purchaser_order, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Order', new_purchaser_order_path %>
